version: '2'
services:
  rails:
    build: .
    image: my/rails5.1
    container_name: 'rails'
    ports:
      - "80:3000"
    volumes:
      - rails-data:/usr/local/src

volumes:
  rails-data:
    driver_opts:
      type: none
      device: ${PWD}/src/
      o: bind
